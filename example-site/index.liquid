<!doctype html>
<html>
	<head>
		<title>{{ blog_name }}</title>
	</head>
	<body>
		<h1>{{ blog_name}}</h1>
		{% for article in articles %}
			<h2><a href="{{ article.page }}">{{ article.name }} {{ article.date }}</a></h2>
			{{ article.summary }}
		{% endfor %}
		{{ refresh }}
	</body>
</html>
